import { CustomAutoFocus } from './custom-auto-focus';

describe('CustomAutoFocus', () => {
  it('should create an instance', () => {
    const directive = new CustomAutoFocus();
    expect(directive).toBeTruthy();
  });
});
